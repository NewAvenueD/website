<!--  one_col.html.twig -->
<div class="row left-three">
  <div class="col">
    <div class="col-contain {{page.header.classes}}">
      <div class="inner">
        {% for row in content|split('<hr />') %}
            <div class="inner-row">
              {{row}}
            </div>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
